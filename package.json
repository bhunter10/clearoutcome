{
  "name": "clearoutcome",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html styles.css script.js public dist/",
    "start": "python3 -m http.server 4173"
  }
}
