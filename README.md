# ClearOutcome Website

Static first version of clearoutcome.com with the ClearOutcome logo, beta opt-in form, placeholder navigation sections, contact information, and footer disclaimer.

The opt-in form opens a prefilled email to `team@clearoutcome.com` so the page works without a backend.
